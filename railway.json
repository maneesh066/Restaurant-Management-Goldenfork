{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn goldenfork.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 4",
    "restartPolicyType": "ON_FAILURE"
  }
}
